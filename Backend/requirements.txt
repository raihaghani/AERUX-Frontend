# ==============================================================================
# Aerux_Final — Intracranial Aneurysm Detection, Localization & Segmentation
# Requirements  |  Python 3.10+
# ==============================================================================
#
# IMPORTANT — PyTorch must be installed SEPARATELY with the correct CUDA wheel.
# Run this first:
#
#   pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu128
#
# Then install the rest:
#
#   pip install -r requirements.txt
# ==============================================================================


# ------------------------------------------------------------------------------
# Deep Learning core  (CUDA 12.8 builds from PyTorch wheel index)
# --extra-index-url adds the PyTorch wheel server alongside PyPI so that
# all other packages (numpy, scipy, etc.) still resolve from PyPI normally.
# ------------------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cu128
torch
torchvision


# ------------------------------------------------------------------------------
# Scientific computing
# ------------------------------------------------------------------------------
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2


# ------------------------------------------------------------------------------
# Medical image I/O & preprocessing
# ------------------------------------------------------------------------------
pydicom==3.0.1          # DICOM file reading
nibabel==5.3.2          # NIfTI file reading/writing
SimpleITK==2.5.2        # Full volumetric processing (N4 bias correction, resampling)
scikit-image==0.25.2    # Sato / Frangi vesselness filters
opencv-python==4.12.0.88  # Image resize, CLAHE enhancement


# ------------------------------------------------------------------------------
# nnU-Net v2  (3D segmentation — new addition)
# Install after the dataset is prepared; requires the env vars:
#   nnUNet_raw, nnUNet_preprocessed, nnUNet_results
# ------------------------------------------------------------------------------
nnunetv2>=2.5.0


# ------------------------------------------------------------------------------
# MONAI  (lightweight alternative to nnU-Net — SegResNet fine stage)
# Install after PyTorch/CUDA is set up:
#   pip install "monai[nibabel]>=1.3"
# ------------------------------------------------------------------------------
monai[nibabel]>=1.3


# ------------------------------------------------------------------------------
# Visualisation & experiment tracking
# ------------------------------------------------------------------------------
matplotlib==3.10.7
seaborn==0.13.2
tensorboard==2.17.1


# ------------------------------------------------------------------------------
# Utilities
# ------------------------------------------------------------------------------
tqdm==4.67.1
PyYAML==6.0.3


# ------------------------------------------------------------------------------
# Web deployment  (radiologist interface)
# ------------------------------------------------------------------------------
Flask==3.1.2
Werkzeug==3.1.3


